import React from "react";

const FilterTask = () => {
  return (
    <div>
      <button>Done</button>
      <button>notdone</button>
      <button>All</button>
    </div>
  );
};

export default FilterTask;
